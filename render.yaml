# Render blueprint — connect this repo in the Render dashboard
# ("New +" → "Blueprint") to create the static site automatically.
services:
  - type: static
    name: future-hide-and-seek
    runtime: static
    buildCommand: ""
    publishDir: public
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
